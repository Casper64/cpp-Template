# cpplint configuration file

# Google code conventions is 80 character line length
linelength=80

# Filter out any rules
filter=-legal/copyright
# filter=-build/include_subdir
filter=-build/header_guard
filter=-build/include_order
# filter=-whitespace/braces
# filter=-whitespace/indent
# filter=-readability/todo
# filter=-runtime/references
# filter=-runtime/string
# filter=-build/c++11

# Root directory for header guard check
# Uncomment and set to your project root if needed
# root=

# Extensions to check (default: .cc, .cpp, .h, .hpp, .cxx, .hxx)
# extensions=cc,cpp,h,hpp,cu,cuh

# Headers to exclude from checks
# Example: exclude_files=.*\.pb\.h$
# exclude_files=
